#include "main.h"
/**
*mul - calculates the products if two numbers
*@a: it is an intger
*@b: it is an integer
*
*Description: returns the product of two inputs
*Return: 0
*/
int mul(int a, int b)
{
return (a * b);
}
